import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'miso-dermoapp-grupo10',
  appName: 'miso-dermoapp-grupo10',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
